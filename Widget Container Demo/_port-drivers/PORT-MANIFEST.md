# Port Manifest - V2 re-port on branch oisin-v2-rebuild

> **STALE HEADER, read this first (2026-09-15).** The rows below were written on branch
> `oisin-v2-rebuild` @ `71ca056`. The working tree has since moved to branch **`OisinBranch`
> @ `9365ba2`**, which carries seven newer `-oc` kinds (budget-oc, pension-oc, payroll-oc,
> remittance-oc, receivables-oc, insurance-oc, deposits-oc) and their drivers, none of which
> have rows here. The W18 row at the bottom is the only one written against `9365ba2`.
> Reconciling the `-oc` series into this table is an open task.

Base: Jo's phase-2 @ 71ca056 (2026-09-07). One widget at a time, strictly serial, per owner instruction.
Rules: W01-W07 = REPLACE our own old -mb blocks with the V2 builds; W09/W10 = fresh adds transplanted
from oisin-v2-port commit 921392e. Jo's code never touched beyond the two dispatch lines per widget.
Titles use the (OC) / (OC, variant) convention. Region comments keep (MB updated) as driver anchors.
W08 My Status and W14 Main Content Tasks are Jo's own, completed by her: never ported, never modified.

| Widget | kind | prefix | status | base | date | notes |
|---|---|---|---|---|---|---|
| W01 Budget Compared to Actual | budget-mb | bgtF | verified | 71ca056 | 2026-09-07 | V2 re-port in place: old block was already V2-shaped, delta = headline arrow+quiet %, info-icon explain, legend+slicer row above charts, single mid gridline, xwide always 2 panels (detail toggle removed), V2 pill/variance cells, (OC) titles, iB default month; driver w01-budget-mb.driver.js 140/140; bar-scroll threshold kept at 13 (Jo card width) |
| W02 Pension Plans | pension-mb | penF | verified | 71ca056 | 2026-09-07 | V2 re-port in place: old block was already 3-view (Table/Pie/By district) and roster-free, delta = v2.2 stale-dist fallback to Table (toggle + content, both tiers), 960px appointees modal made a deliberate flex row (table majority + 190px right rail, scoped to our penf-detail-modal class so Jo's pen modal cascade is untouched), (OC) titles; driver w02-pension-mb.driver.js 159/159; donut kept at Jo's 200px (Final's 180 was an fc-card proportion trim, not a Step 4 delta) |
| W03 Payroll Distributions | payroll-mb | prF | verified + diff-doc'd | 71ca056 | 2026-09-07 | V2 re-port in place (v2.6): old prScope scope chip and by-pay-type grouping view REPLACED by two independent filter chips (prDist/prPT, third triggerSelector/popContent branch added on our own dispatch lines); nested in-widget drill dist->employees->pay types (v2.2, no link-out, no drill modal, prFOpenDist/prF-drilldist removed); row-level PRF_PAYROLL_PERM gate; amt-desc default sort, name/amt keys only; ONE view-scoped export (btn naked sm, data-prf=export-view; shell's GLOBAL .btn family satisfies the FC v2.6 .prf-root .btn fix); glance = distribution total; no comparison fields (F3); old prf-g* glance bar + wt-rowlink/leg-link CSS removed; (OC) titles, prF2 demos the pay-type filter (Overtime); driver w03-payroll-mb.driver.js 277/277; W01 140/140 + W02 159/159 re-verified after edit |
| W04 Remittance Pledges | remittance-mb | remF | verified + diff-doc'd | 71ca056 | 2026-09-07 | V2 re-port in place (v3.5a), the biggest delta of the run: exception repositioning (behind-pace shortfall headline, per-row summed, N of M pill, goal pill gone); Jo's three pace cards over a FLAT table with Behind = shortfall > 0 (deliberate departure from her 30-day band; cards assert-agree with the header under every range); receipts-only activities = footer line, never filtered; receipts WINDOW (This year / Last 30 days / Custom From-To, end paces cumulatively, start scopes pledges + windowPaid, no Refresh); drill SCREEN on Jo's modal shell (8/page pager, cents-reconciling) with third-level per-pledge payment schedule (oldest-first allocation, first skipped date named); top-5-behind popup on bars rows (v3.4 arity fix in); exports view-scoped; REMF_USE_POPUP rollback intact; hover namespaced data-remfpop (Jo's .pbar[data-pbar] listener untouched); Jo's remHandleInput restored to single-path (old remFq- fork removed); v3.5a split-selector guard in driver; (OC) titles remF/remF_k/remF2/remF3; driver w04-remittance-mb.driver.js 329/329; W01 140/140 + W02 159/159 + W03 277/277 re-verified after edit |
| W05 Receivable Invoices Outstanding | receivables-mb | arF | verified + diff-doc'd | 71ca056 | 2026-09-07 | V2 re-port in place (v2.3): SIX bands (Current not-yet-due hi:0 / 1-30 / 31-60 / 61-90 / 91-120 / 121+, six-step ramp led by --am-100), overdue = days>0, every band renders (zero rows inert, old "No balance in" collapse gone), 23-invoice dataset (Rule 11), customer rollup paged 7/page + sortable (Biggest amount / Oldest balance, clamped page, filter/sort resets), PIE third toggle segment on the shell's GLOBAL pie primitives (only arF geometry CSS added; 300 Explore / 210 Detail, centred per v2.3), toggle now renders at Detail (left panel follows it), modal checkbox restyled to V2 bordered button + Confirm note only AFTER Confirm (old arF-dcheck glyph + pre-Confirm live count removed); modal tabs ported as built (data sources = open Feargal item, NOT resolved); overdue pill tooltip kept as built ("more than 30 days" wording predates v2.1 maths, flagged in diff doc); (OC) titles, arF2 = Detail by customer; harness matchSel widened to uppercase attr names (data-arFpop); driver w05-receivables-oc.driver.js 288/288; W01 140/140 + W02 159/159 + W03 277/277 + W04 329/329 re-verified after edit |
| W06 Insurance Billing Plans | insurance-mb | insF | verified + diff-doc'd | 71ca056 | 2026-09-07 | V2 re-port in place (v2.4): nested Type->Plan table now the only table (old flat Explore table + plain Share head + "Total enrolled" footer gone); "Share of total" head (v2.1 Feargal); "Total (all types)" footer with enrolled + cost; legacy PIE restored (v2.2) on the shell's GLOBAL donut/legend primitives, amethyst INSF_COLORS --am-600..--am-200 keyed to fixed plan order; Explore = Table / Pie segment, TABLE default (pie-as-default never owner-confirmed, carried open, not flipped), stale insView falls back to Table pressed; Detail = BOTH at 58/42 with a dividing rule, no segment; zero-enrolment plans uncharted + note; chart-empty state names the filter; legacy hover titles (segment + legend), no drill; a11y: donut role=img + aria-label, per-segment title, legend counts/percents as DOM text; v2.4 measured sizing CSS (donut flex-basis 320/240, legend caps 250/200, flex-wrap, centred slack); only type change fetches; (OC) titles insF/insF_k/insF2/insF3/insF4; driver w06-insurance-oc.driver.js 280/280; W01 140/140 + W02 159/159 + W03 277/277 + W04 329/329 + W05 288/288 re-verified after edit |
| W07 Deposit Accounts (Deposits on Hand) | deposits-mb | depF | verified + diff-doc'd | 71ca056 | 2026-09-07 | V2 re-port in place (v2.0): old block was already built from v2.0 so the delta is modest (101 changed lines vs snapshot): table pager now the built page-N-of-M shape (prev/next + "Showing X to Y of Z accounts", tpage state; old Load-more/tshown/depf-more gone, stale unused .depf-dp-* CSS removed); own empty/loading/error states with the empty trigger on the account COUNT (zero accounts, Step 5 v2) or a state flag, dispatch moved BEFORE the generic empty fallback, dataset flag renamed none->empty; trend LINE click now drills like the legend (type re-scopes + By Account, account inert): canvases carry data-mode + data-multi="0" to opt out of the shared isolate-on-click, own capture listener reads shared lnActive (hover still piggybacks Jo's shared lnUpdate/trHighlight, table search still piggybacks her shared .dep-q filter, row modal still her shared modal.b path mirroring her own acctm body); modal table row keeps the account-number disambiguator (gap #14); donut Others slice carries data-n/data-kind; Escape closes the popover; decorative icons aria-hidden; (OC) titles depF/depF2/depF3/depF4/depF5 + system s1; Compare-To trend-overlay doc-vs-build conflict CARRIED as flagged (port follows the build: no overlay); driver w07-deposits-mb.driver.js 224/224; W01 140/140 + W02 159/159 + W03 277/277 + W04 329/329 + W05 288/288 + W06 280/280 re-verified after edit |
| W09 Payroll Scheduled Time Off | pto-mb | ptoF | verified + diff-doc'd | 71ca056 | 2026-09-07 | transplanted unchanged from 921392e (5 pieces: 666-line code block incl self-contained PTOF_WIRED handler subsystem, 209-line CSS block, 6 registry lines with (OC) titles, contentHTML dispatch before the generic empty fallback, aboutOf dispatch); no donor logic modified, no anchor adaptation needed beyond current-file positions; collision re-check vs 71ca056 clean, Jo's ptoFindEmp/ptoFindRec/ptoFmtDate byte-verified unchanged; driver w09-pto-mb.driver.js 411/413, the two remaining assertions check coexistence with the W10 block, not yet re-ported on this branch, and will pass once W10 lands; W01-W07 drivers re-verified after the edit (140/159/277/329/288/280/224) |
| W10 Loans With Balance Due | loans-mb | lonF | verified + diff-doc'd | 71ca056 | 2026-09-07 | transplanted unchanged from 921392e (5 pieces: 499-line code block incl self-contained LONF_WIRED handler subsystem with its own #lonPop and #lonModalRoot, 182-line CSS block, 6 registry lines with (OC) titles, contentHTML dispatch line, aboutOf dispatch); no donor logic modified; anchor adaptations were position-only: CSS block placed BEFORE the W09 CSS block and contentHTML dispatch BEFORE the pto-mb line, both donor-order positions required by W09's coexistence guards; collision re-check vs 71ca056 clean, Jo's loan block and .loan-c-acct/.loan-head/.loan-body/.loan-band-row CSS md5-verified byte-identical, only prior lonF/lonType mentions were comments inside the W09 block; driver w10-loans-mb.driver.js 228/228; FINAL nine-driver tally all green: W01 140/140, W02 159/159, W03 277/277, W04 329/329, W05 288/288, W06 280/280, W07 224/224, W09 413/413 (its two W10-coexistence guards now pass), W10 228/228 |
| W11 Fixed Asset Values | fixedassets-mb | faF | verified + diff-doc'd | 71ca056 | 2026-09-07 | FIRST-EVER port. Our Final is the 2026-09-03 from-scratch rebuild (build 3.6), NOT a restyle of Jo's block (her earlier-derived port was deleted by owner instruction after six failed restyles). Two views behind a toggle (Asset Detail table + Donut; Group Bars removed 09-03), Detail = ONE view full width (side-by-side pair retired), server paging FAF_PAGE_SIZE 10 with totals over the whole group, all 7 columns server-sorted (default tag-asc), Financial Measure chip Donut-view-only, icon-only download, 52-asset register vs Jo's 13. Jo's own fixedassets widget (faContent + her fa* helpers, data-action fa-group/fa-groupby/fa-measure) byte-untouched; our namespace is data-faf + .faf-* + faF/FAF_, zero collisions. (OC) titles x7. DEFAULT VIEW CONFLICT CARRIED: handoff says Donut, built code treats null as Asset Detail; port follows the build, flagged (Sign-off row 10). Register is mock data, six-dimension/five-measure feasibility unconfirmed, three account dimensions return empty lists, J1-J7 Unreviewed. driver w11-fixedassets-mb.driver.js 435/435; all nine earlier drivers re-verified after the edit (140/159/277/329/288/280/224/413/228) |
| W13 Purchasing Management | purchasing-mb | purF | verified + diff-doc'd | 71ca056 | 2026-09-08 | FIRST-EVER port, built FROM Jo's purchasing block per the clean-port rule. Our Final = purF v2.6 (FC_VERSION[13], all rounds 2026-08-19). **COLLISION CLEARED: Jo's OWN purForStatus / purPathsForStatus / purFiltered / purFindOrder all start with the letters purF and are HERS.** Every one of our names is purF plus a CAPITAL, so none can shadow one of hers; after the port each of her four is still defined exactly once and byte-identical, her whole 35,644-byte purchasing block and her 12,468-byte .pur-* CSS cluster are byte-identical, and our CSS block declares zero .pur-* selectors (PURF_ 101 / purf- 309 occurrences, all ours). Namespace: kind purchasing-mb, data-purf / data-purf-ref / data-purf-card / data-purf-drop, .purf-*. WHAT IT IS: the Kanban IS the approval process, 3 real approval states (Pending / Approved / Rejected) replacing her 4 hash-derived payment lanes; the v2.6 SPLIT FINISH column (Close = paid only on top, Void = unpaid only below) shown ONLY at All statuses; drag-to-action behind a Reason popup with Confirm/Cancel and same-column no-op; HOLD (yellow card + lock + reason, undraggable, and the Approvals grid refuses approval changes on it); card colour carries the payment sub-state always paired with a text badge; record-screen-parity modal (5 tabs, interactive Approvals grid driving the SAME guards as drag, invoice payment entry, Update + Cancel); PO Table second view on an always-visible header toggle, its status list adding Closed/Voided; encumbrance chart CUT so her data selector/chart/chart-table/bar-hover have no counterpart; FIXED oldest-first sort everywhere with no sort control; caps 2 board / 5 table Explore / 10 table Detail with +N more; headline = the Pending count 'N pending approval', path-scoped and deliberately NOT status-scoped; Department/Year/Overdue-only are Table-and-Detail-only Rule 11 chips that say so in their own popovers. SHELL CONFORMANCE (not design changes): global PURF_UI -> per-widget registry state, native selects -> her .filter-chip + .dd-search/.menu-scroll, inline onclick/ondrag -> delegated data-purf behind PURF_WIRED, real <table> -> her .wt-row primitives carrying ARIA table roles, showToast -> setStatus, own purFMoney at two decimals (Step 4 rounding rule; money() would print $87.50 as $88), headline rendered as our own .dep-hd KPI line because titles here are static registry strings in Jo's titleHTML. Record-modal form fields are the one piece of new chrome (no text-field primitive in the shell), derived from her .pur-po-note declarations and FLAGGED. CARRIED, NOT RESOLVED: the Feargal C3 payment dispute (redirect vs the built in-widget entry) with the Step 5 v2 endpoint gated DISPUTED and the tab saying so on screen, no side taken; voided-request financial treatment recorded both sides with no reversal asserted; dossier 11.6 and the stable-columns half of 11.5 Unreviewed; Department/Year/Overdue UNVERIFIED; no write API for approve/reject/hold/payment/archive (rows 9-13); Rejected needs the API to start serving rejected orders; Feargal items 1/2/4/6 documented not built (the Approvals tab names the gap); drag is pointer-only (Enter on a table row is the keyboard route); volume ceilings TO CONFIRM. driver w13-purchasing-mb.driver.js 763/763 (incl. a 180-combination size x view x status x path no-em-dash sweep and explicit byte-checks on Jo's four lookalikes). One real cross-widget catch and fix: a CSS comment of ours quoted her .filter-chip[data-action] rule verbatim and pushed the count the W09 driver guards from 3 to 4; the comment was reworded rather than the guard loosened. FINAL ELEVEN-DRIVER TALLY all green: W01 140/140, W02 159/159, W03 277/277, W04 329/329, W05 288/288, W06 280/280, W07 224/224, W09 413/413, W10 228/228, W11 435/435, W13 763/763 |
| W15 Bank Balances | bank-mb | bkF | verified + diff-doc'd | 71ca056 | 2026-09-08 | FIRST-EVER port, built FROM Jo's bank block per the clean-port rule. Our Final = bkF, **REBUILT 2026-09-04** ("change the design to Jo's, dont copy it"); the 2026-08-30 v2.0 bankF port was discarded 2026-09-03 and is NOT resurrected. **No FC_VERSION[15] exists in the build, so the bkF block's own dated comments are the only version record and are what the docs cite.** COLLISION: her widget is kind "bank" with 26 bank*-named helpers + BANK_ACCOUNTS + a 107-rule .bank-* CSS cluster; none start with bkF, and bkF / BKF_ / data-bkf / .bkf- / bankf were each ZERO before this port. After it: all 26 of her functions defined exactly once, BANK_ACCOUNTS declared once, her 5 kind:"bank" registry rows byte-identical, her 17,144-byte render block and 1,417-byte hover tail md5-identical, all 107 .bank-* rules (9,266B) byte-identical, EMPTY_COPY.bank / ERROR_COPY.bank untouched. CSS REUSE IS THE HEADLINE: 74 of her classes are already declared, so our block adds only 51 rules (21 .bkf-* classes + three groups of forced .bkf-root-scoped overrides) and declares ZERO .bank-* selectors, unlike our Final's stylesheet which re-implemented her whole vocabulary because our mockup had none of it. WHAT IT IS: TWO presentations (Balance Table default + her diverging Balance Bar Chart; Account Cards REMOVED 2026-09-04), one at a time at every tier (no xwide pairing, unlike her bankAllFull); SERVER paging/ordering/aggregation via bkFServerQuery at page size 12 over 52 accounts, replacing her Load-more + client sort/slice/total, with full-set totals and maxEnding/minEnding so turning the page moves neither the total nor a bar's length; fixed alphabetical order, whitelisted sortBy (one member), unique id tiebreaker, page index clamped both ways; SORTABLE COLUMN HEADERS DELIBERATELY UNBUILT (open item 7, owner's); Overdrawn-only chip reusing HER .bank-odchip unrestyled, count over the whole ACTIVE set while rows/totals/pageCount follow the FILTERED population, cleared in Single Account mode, ABSENT at Glance; Glance = one always-aggregate figure, no controls (hers is interactive with a drill chip); seven-row breakdown with row 7 inside the table and NO totals row (hers is an 8th row outside it) plus per-row provenance sub-lines; four-category activity chart at Detail only on her .bank-cols chart; native <table> with th/scope/caption because a th set via innerHTML in a div[role=row] is a silently-dropped parse error; no download (removed 2026-09-04, no actions array), no search, no drill overlay, no per-bar hover card, no Load more; polite live region announcing the mode switch; data-tier on root and chart. TWO REAL CATCHES, both fixed: (1) **Jo's live zero-axis defect reproduced in her own shell** , her .bank-hb-zero reads var(--wn-500) and --wn-500 is declared NOWHERE in the file, so her diverging bar's axis is invisible in her build; her rule is left exactly as it is and only our axis is re-pointed to --wn-750, and the driver asserts both the defect and the fix. (2) **the Final's own inline comment misstated its fixture** , it said four accounts end negative when the arithmetic over the same numbers gives SEVEN; the comment was corrected, not the data. Also, two of our comments quoted the very tokens their guards count (a .sort(/.slice( claim and a verbatim quote of her flex row rules), the exact W13 hazard: both were REWORDED rather than the guards loosened. Red stays off the bars per Styling Reference 8.2 (--am-500 in / --am-700 out vs her --red-100), overdrawn carried by axis side + glyph + the word Overdrawn; red survives only in the status chip. SHELL CONFORMANCE (not design changes): the Final's global BKF_STATE -> per-widget registry state (bkFAcct/bkFView/bkFPage/bkFOver/bkFSort, none colliding with her bkacct/bkview/bksort), own delegated listeners behind BKF_WIRED keyed to data-bkf with its own #bkFPop, and bkfRerender -> her global render(). (OC) titles x7. CARRIED, NOT RESOLVED: the paged accounts endpoint exists in NO Modern API doc (central NEW backend work); the seven-line breakdown is entirely NEW and is the WAIVED Rule 11 ask that blocks Single Account mode (Sign-off row 3, the only blocking row); ALL of Jo's dossier findings are Unreviewed with no reconciliation file, including her live pound-sign localisation defect and the available-vs-unrestricted-cash "Do now" (gap 10.4), plus multiple-instances/saved-default (10.5) and labelling the unreconciled-only basis; the SME attribution conflict (her dossier "Marvin" vs Step 2's Ben Lane, same 13 Jul interview) recorded both sides; Ben Lane's top-3-5-plus-view-all preference vs the built paged full set is an open design question, port follows the BUILT behaviour and flags it (open items 2 and 4); negative balances in the bar chart undefined (item 6, named on screen in one conditional muted line); loading unspecified and NOT invented (item 8, her bankSkeleton/bankLoad left to her); Last Reconciled visibility (item 1) and unreconciled-item count (item 10); the always-aggregate Glance exception (item 5). driver w15-bank-mb.driver.js 463/463 (incl. a 96-combination size x view x filter x account x dataset no-em-dash sweep, per-page total/scale invariance across all five pages, and 26 byte-checks on her helpers). FINAL TWELVE-DRIVER TALLY all green: W01 140/140, W02 159/159, W03 277/277, W04 329/329, W05 288/288, W06 280/280, W07 224/224, W09 413/413, W10 228/228, W11 435/435, W13 763/763, W15 463/463 |
| W16 Accounts Payable By Due Date | payables-mb | apF | verified + diff-doc'd | 71ca056 | 2026-09-08 | FIRST-EVER port, built FROM Jo's payables block per the clean-port rule. Our Final = apF v2.0 (FC_VERSION[16], built 2026-08-19 as a 1-to-1 copy of her v2 ap design per dossier 7371554882 Part C 11.1/11.3/11.4/11.5, PLUS one owner addition), Step 4 audited 2026-09-07. **COLLISION CLEARED, the sharpest yet: Jo's OWN apFmtLong / apFmtShort / apFull / apSelLabel begin with the SAME THREE LETTERS as our prefix and are HERS** (the ptoFindEmp / purFiltered trap, worse because the letters match exactly). The rule that clears it is a DOUBLED F: ours are apFFmtLong / apFFmtShort / apFFull / apFSelLabel, so no name of ours can shadow one of hers. APF_ and .apf- were each ZERO before the port. After it: all 38 of her ap* helpers defined exactly once, every definition line byte-identical to the snapshot, the raw counts of all four lookalikes unchanged (6/2/2/4), AP_INVOICES / AP_INVOICES_SINGLE / AP_BANDS / AP_TODAY declared once each, her six kind:"payables" rows untouched, EMPTY_COPY.payables and ERROR_COPY.payables unedited (our empty strings are inline, never written into her shared copy object), and not one of our 57 top-level names equals one of hers. Namespace: kind payables-mb, data-apf with apF- VALUES (never her ap-*), .apf-*. CSS REUSE IS THE HEADLINE: she already declares the entire .ap-* cluster our markup draws with (the two-line .ap-c-due cell and the tinted .ap-grp band subheader included) plus every shell primitive, so our block adds only NINE rules, all .apf- scoped, declaring ZERO of her selectors; the one rule mentioning hers is .apf-root .ap-sum-row[data-apf]:hover, needed only because her own hover keys on [data-action], which our rows deliberately do not carry. WHAT IT IS: her v2 design plus ONE owner addition, a due-date HORIZON chip (All outstanding / Next 7/30/60/90 days) that scopes the working set with OVERDUE ALWAYS INCLUDED and a SNAP RULE returning the due filter to All due dates when a horizon empties or invalidates it; filtering is WIDGET-WIDE via a single apFData(w) scoper (the raw array is reached in exactly four places), so KPI total, overdue pill, context line, band subheaders, table, cash panel, vendor rollup, popover amounts and footer cannot disagree; Glance is filterless but its KPI is horizon-scoped, not unconditional; NO VIEW SWITCH anywhere (no charts, no donut, no timeline, no cards, and the .dep-hd-toggle slot renders once and stays empty); aging-band grouped hero table with per-band counts and subtotals reconciling to the footer to the cent, flattening when a band or a date is picked; FOUR columns, her fifth Status column folded into the two-line due cell; all four sortable with aria-labels announcing direction, due-asc default and LARGER-AMOUNT tiebreak (never vendor-alphabetical); Detail adds her Cash requirements selector panel + Top vendors owed; vendor search past 6 rows also hides band subheaders whose rows all filter out; "Nothing outstanding" empty state, short variant at Glance, dispatched BEFORE the generic fallback; a due-filter or horizon change is the ONLY fetch (800ms, spinner on whichever chip started it), sorting never fetches. NO DRILL-THROUGH: her ap-open invoice modal is deliberately not carried (Sign-off row 1). FOUR REAL FINDINGS IN HER OWN FILE, all reported and NONE fixed: (1) her apHeaderBlock ASSIGNS the loading line "Updating outstanding invoices..." to a local and never renders it, so her own loading state is a bare skeleton with no explanation (ours renders it, with three ASCII dots rather than her U+2026 ellipsis); (2) her registry carries an apview key her v2 block never reads (w.apview = 0 occurrences), with ap3 setting it to "timeline", a view her v2 does not have; (3) apDueWithin and apISO are defined and never called; (4) .ap-glance-cap is declared in her CSS and used nowhere. SHELL CONFORMANCE (not design changes): the Final's global APF_STATE -> per-widget registry state (apFDue/apFSort/apFHz/apFLast/apFLoading, none colliding with her apDue/apsort/apview), our own delegated listeners behind APF_WIRED keyed to data-apf with a body-mounted #apfPop, apFRerender -> her global render(), the fetch timer keyed timers["apF-"+id] in her shared timers object, our own apFIcon dropped in favour of her ICON helper, and the Final's native title= on the overdue pill replaced by her delegated data-tip/data-tip-plain tooltip system (no native title anywhere in the block). (OC) titles x6; apF4 uses dataset:"none" for the empty state and her single-date AP_INVOICES_SINGLE fixture is not carried (the same state is reachable by picking any specific date), the sixth entry demoing a horizon-scoped Detail card instead. CARRIED, NOT RESOLVED: **THE AGING BASIS (due date vs invoice date) WAS NEVER CONFIRMED AGAINST THE REAL API and is load-bearing for every band** , the spec's top open item; bands run off the DUE date as her band helper does, confined to one function so a flip has one edit site, and the W10 "Over 60 means 90+" mislabeling carryover is unconfirmed per widget. The ACTIONABILITY DISPUTE is recorded both ways with no side taken: Jo/SME (dossier 11.2) want pay/schedule on the widget, Feargal ruled 2026-08-25 it stays a focused overview with separate action widgets (Aditya, outside this queue), so the port is READ-ONLY and the driver asserts no pay/schedule/approve control exists. Dossier 11.3/11.4 (a date-ordered chart the Final does not have; the sync is realised as synced values-as-text via the band selector rows) and 11.6 (Bank Balances pairing) are Unreviewed, with no reconciliation file and no Accepted/Rejected/Disputed status on any of her flags. Entitlement is NOT enforced server-side (metadata-only module access, any authenticated user can call the endpoint) and is nothing simulated here. The GBP localisation defect (legacy hard-codes a pound sign for a US org) and data freshness are NEW/UNVERIFIED backend work; en-US dollars is a build choice, since Step 4 records rounding/currency/locale as not yet specified. **The div-table accessibility question is ported AS BUILT and flagged:** the hero table is div-based .wt-row with no table/columnheader roles and no th/scope, exactly as she built it, while chips, sort buttons and both listbox popovers do carry ARIA; whether to require role=table parity (as W11 built, and W15 went further with native tables) is [TO CONFIRM - Oisin], and it is the one place this port knowingly ships less than two siblings. Drill-through has no verified target or URL pattern and nothing is built. The by-vendor donut is DEFERRED not rejected (the Top vendors owed rollup is the by-vendor cut that ships). Empty-bucket treatment was never decided: a recorded build choice keeps the disabled "nothing due" row in the panel for layout stability but omits the band from the popover and the table. The Phase 2 ap-ar-aging endpoint uses Current/1-30/31-60/61-90/91+ buckets, not these four bands. driver w16-payables-mb.driver.js 740/740 (incl. a 198-combination size x band x horizon x state no-em-dash sweep, rendered-markup band-subtotal reconciliation at two horizons, overdue-always-included asserted at all five horizons, both snap-rule directions plus two no-snap cases, and definition-level byte-checks on all 38 of her helpers with explicit guards on the four lookalikes). Four driver assertions were self-corrected during the run rather than loosened: two were regex imprecision of mine (a nested sort-button helper counted as top-level, and a scoped-hover selector read as starting at one of hers), and two were pre-existing counts of 2 verified against the snapshot, where HER OWN wiring comment quotes her dispatch line and her empty-copy assignment. FINAL THIRTEEN-DRIVER TALLY all green: W01 140/140, W02 159/159, W03 277/277, W04 329/329, W05 288/288, W06 280/280, W07 224/224, W09 413/413, W10 228/228, W11 435/435, W13 763/763, W15 463/463, W16 740/740 (4,739 assertions total) |
| W17 Gifts Pledges | gifts-mb | gpF | verified + diff-doc'd | 71ca056 | 2026-09-08 | FIRST-EVER port and the LAST of the run, built FROM Jo's gifts block per the clean-port rule. Our Final = gpF, FC_VERSION[17] 1.3, Step 4 audit-stamped 2026-09-07. **THE HEADLINE IS THE W04 DECOUPLING.** In our mockup the gpF root carried BOTH `remf-root gpf-root` and leaned on `.remf-root .btn` for its export button, and the coupling ran far deeper than the button: our mockup also drew on the whole `.rem-pl-*` pledge-table/pager family, `.rem-legend*`, `.rem-pledge-panel`, `.rem-caret`, `.rem-cap`, `.rem-behind-sec`, `.rem-name-txt`, `.rem-pop-dates`, `.rem-spin` and `.remf-chip`, every one of which exists ONLY inside our own live W04 block scoped under its root. ALL DROPPED: our root is `.gpf-root` ALONE, and the export button resolves from the shell's GLOBAL `.btn` family (`.btn` + `.btn.naked` + `.btn.sm` + `.btn.primary` + `.btn.outlined`), the same thing W03's port relied on. The driver asserts it four ways: no `remf-` string in our output at any tier, popover and modal roots ours alone, `.btn`/`.btn.naked`/`.btn.sm` each declared BEFORE the first ported block (so genuinely global, not W04's), and the old `.remf-root .gpf-export` rule gone from the file. A SECOND FINDING FROM THE SAME AUDIT: four selectors our mockup relied on were never declared ANYWHERE in her file (`.rem-ctlrow`, `.rem-empty`, `.remf-vtoggle`, `.remf-mb`), so the mockup had been shipping unstyled chrome; replaced with her `.gft-ctlrow` / `.state` / `.vtoggle` / `.modal-backdrop`. COLLISION: her widget is kind "gifts" with 36 `gft*` helpers plus `GFT_*` constants and a `.gft-*` cluster; NO letter overlap with `gpF`, so unlike W09/W13/W16 there was no lookalike trap. `GPF_` / `.gpf-` / `data-gpf` / `gifts-mb` were each ZERO before the port (the only two `gpF` strings were comments in our own W04 block about this widget's rollback pattern). After it: all 36 defined exactly once, every definition line byte-identical, every raw occurrence count unchanged, her 36,226-byte gifts block and 9,096-byte CSS cluster md5-identical, her six kind:"gifts" rows and all `GFT_*` constants and `EMPTY_COPY.gifts`/`ERROR_COPY.gifts` untouched, no non-namespaced global leaked. CSS REUSE IS THE OTHER HEADLINE: three of her grids fit ours EXACTLY (her six-slot `.gft-trow` = our six baseline columns; her eight-slot `.gft-drow` = our caret + seven donor columns; her `.gft-prow` = our goal bar), so our block declares ZERO of her selectors as a rule subject, every rule is `.gpf-root` scoped, and exactly THREE rules name one of her classes as a scoped descendant (two-line name cell, third Reference column, wrapping legend with counts). WHAT IT IS: the GOAL-PROGRESS side of the pledge pair (W04 owns the exception side, not blurred); goal bars + the live product's exact baseline table, ONE view at a time full width at Explore and Detail (v1.2); Glance is the only overall goal read; campaign filter NARROWS with NO highlight (six highlight classes asserted absent, and a filtered bar's markup byte-identical to its unfiltered self); bar click opens the top-5 most-behind modal; row expands the donor breakdown INLINE at 20/page with totals invariant across pages; pledge click expands its GIFT transactions; bars closest-to-goal-first, drill most-behind-first, table in DATA ORDER with NO sort control and NO alphabetical sort anywhere (a deliberate departure from her sortable headers); export is the ONLY action at two scopes, both Rule 11 stubs. THE DONUT IS RETIRED VIA THE ROLLBACK PATTERN AND THE DRIVER PROVES IT THREE WAYS: `gpFDonut` stays defined and correct with its CSS retained, but no toggle segment offers it, `gpFView` maps any stale "donut" back to "goal", no registry entry seeds it, and seven view values incl. a forged donut event all render no donut. Same for `gpFGoalPanel` behind `GPF_V12_LAYOUT`. SHELL CONFORMANCE: global `GPF_STATE` -> nine per-widget `gpF` registry keys, `gpFRerender` -> her `render()`, `showToast` -> her `setStatus`, own `gpFIcon` dropped for her `ICON`, timer keyed in her shared `timers`, own `gpFMoney` KEPT at two decimals (her `money()` prints whole dollars and this widget's verified figures carry cents), style (b) handlers behind `GPF_WIRED` so her renderModal/popContent/triggerSelector are untouched, empty dispatch BEFORE the generic fallback. Colour: blue = `var(--brand-100)` (which IS the Final's #4b6ec3 to the byte), goal-met = `var(--pos-100)`; the middle band stays a hex because the shell declares NO amber/saffron token, exactly as she hard-codes her own saffron. (OC) titles x7. THREE REAL CATCHES: (1) two of our comments quoted her `gftThru` verbatim, pushing its count 26->28, the exact W13/W15/W16 hazard: both comments were REWORDED, never the guard loosened, and the count is back to 26. (2) our own Final had a latent arity bug, the modal mapped its row renderer straight through `map` so the array index arrived as the widget and the array as the date, silently breaking the pledge expand in the modal; every call site now passes all three args (same class as W04's v3.4 fix). (3) TWO LATENT GAPS IN HER OWN BLOCK, reported and NOT fixed: her `class="state gft-empty"` uses `.gft-empty`, which her stylesheet never declares, and her `.gft-total-row` resolves only to a generic shell rule. CARRIED, NOT RESOLVED (the most of any widget in the run): **the Received basis conflict is the TOP item and blocks EVERY summed figure** (build counts pledge-linked gifts only per owner decision 08-19, the Modern API and the campaign DTO's TotalRaised count all posted gifts; confined to one function so a flip has one edit site); the Step 4 doc's OWN internal inconsistency about % Due (CONFLICT row says disputed and blocking, Sign-off #2 says RESOLVED with live proof 904/1000=90.40% and -655/1200=-54.58%, build follows the proof) recorded and NOT collapsed; goal-met colour contested (build/Step 4 green vs the comparison doc's "red (goal met)" enum value, sidestepped by serving raw percent); GF_Campaign vs GF_Purpose keying for Goal [TO CONFIRM]; the "Open in Gifts and Pledges" stub against the export-only constraint [TO CONFIRM - Oisin] (it predates v1.3 and the Final's banned-verb assertion never covered "Open"); ALL of Jo's dossier flags Unreviewed with no reconciliation file, incl. the pound-sign localisation defect and the % Due -> % Fulfilled relabel (NOT applied, so the two widgets differ on screen); four v1.2 judgement calls (bar cap lifted, trim removed, counts in legend, best-first order); volume ceilings, legacy ORDER BY and the pledge term/frequency/installment column names all unverified. Also repaired a pre-existing structural defect in the Confluence diff doc: the W15 and W16 runs appended their sections AFTER `</body></html>`, leaving ~160 lines outside the document; the two closing tags were moved to the true end with no section's content altered. driver w17-gifts-mb.driver.js 734/734 (incl. a 155-combination size x campaign x view x drill x loading no-em-dash sweep, totals invariance across every page, both page-clamp directions, and definition-level byte-checks on all 36 of her helpers). Six driver assertions were self-corrected during the run rather than loosened, all six mine being imprecise: the bars-only claim matched the view toggle's own tooltip naming the table's columns (re-anchored on body markers), a popover-toggle sequence that had already been closed by the preset it chose, a keyboard path that failed only because the harness's generic node.click() rebuilds a target from the action alone and drops its other attributes (given a faithful target that re-dispatches its own bag, as a real DOM does), an out-of-range page expectation that contradicted the built clamp-on-read behaviour, and a generic-empty-fallback anchor that matched an EARLIER widget's own empty branch instead of the real generic one (the actual dispatch order was correct all along). FINAL FOURTEEN-DRIVER TALLY all green: W01 140/140, W02 159/159, W03 277/277, W04 329/329, W05 288/288, W06 280/280, W07 224/224, W09 413/413, W10 228/228, W11 435/435, W13 763/763, W15 463/463, W16 740/740, W17 734/734 (5,473 assertions total) |
| W18 Financial KPI | (none, not a registry widget) | fkp | verified | 9365ba2 | 2026-09-15 | FIRST-EVER port of a widget that is not ours and not Jo's: Aditya's "Financial KPI Cards v5" (`Aditya_Widget_Design/Dashboard Container Demo.html`, `#w-kpi5`, spec dated 2026-09-15), pulled from branch `feature/aditya-widget-design`. **THE READINESS GATE IS WAIVED, NOT PASSED** , by owner ruling 2026-09-15 this widget is standalone: there is no Step 3 Final (no `fc-widget-18`, `WRENDER` stops at 17), no `Step 4 - Widget Final Design/W18` doc and no Step 5 spec, and none is required. Every decision below traces to Aditya's own 16 KB spec or to a convention of this shell; nothing was invented to fill a gap. Note the project's own `Widget_Specs/General Widget Design Rules.md` still records W18+ as an undecided product question. **IT IS NOT A GRID CARD.** Owner ruling: a FIXED full width band pinned above the widget grid at Glance height, no resize, no kebab, no rescaling (which matches Aditya's own v3 comment, "Always-top, no resize/kebab"). So it has NO registry entry, NO `kind`, NO tiers and NO `contentHTML()` / `aboutOf()` dispatch, making it **the only ported widget in this file that edits none of Jo's JavaScript at all.** The single shared-region edit is one static line, `<div class="fkp-band" id="fkpBand"></div>`, inserted between `#status` and `#dashboard`; the block mounts itself there and repaints only itself, so her `render()` is not modified to call it. Band height is DERIVED from her own grid, not guessed: her `.widget[data-size="kpi"]{grid-row:span 3}` against `grid-auto-rows:48px` + `gap:16px` gives 3*48 + 2*16 = **176px**, with her 16px gap as the bottom margin. COLLISION: the cleanest of any port in the run , `fkp`, `FKP_`, `data-fkp`, `.fkp-`, `fkpBand`, `W18`, `Financial KPI`, `kpi-card` and `--sf-` were **each ZERO** before the port, so there was no lookalike trap of the `ptoFindEmp` / `purFiltered` / `apFmtLong` class at all. After it, her `render()` is byte-identical, her `contentHTML()` opening is byte-identical, every one of her `kind==="..."` branch counts is unmoved, and all of `widgetHTML` / `money` / `setStatus` / `ICON` / `aboutOf` are still defined exactly once. Diff is **605 added, 0 deleted**, CRLF throughout with zero bare LF. ARCHITECTURE REBUILD (this was the real work, not restyling): the prototype was static markup driven by direct DOM writes into singleton ids (`applyReconState5` setting `innerHTML` on `#kpi5-warn-wrap` and `#kpi5-val`), which cannot coexist with a shell that renders the same kind many times; rebuilt as `FKP_STATE` plus one pure `fkpBandHTML()` render pass, 2 inline `onclick=` attributes replaced by delegated `data-fkp` behind `FKP_WIRED` (style (b), the depF/lonF/bkF precedent), `role="button"`+`tabindex` divs replaced by real `<button>`s so Enter and Space work natively, and the prototype's global `.kpi-warn-wrap` document sweep narrowed to our own tile so it can never reach another widget. COLOUR IS THE HEADLINE. His amber system (`#e67e22`, `#b7620a`, `#fff8e1`, `#eed5b0`, `#fff3e0`, `#f0c07a`) has no token in this shell, and per owner ruling "use the Jo design folder/token if possible, else the closest thing". Resolved in two tiers. Where she declares a token it is used, and three are EXACT to his hex: `--cn-190` = his `#202020`, `--cn-130` = his `#606060`, `--pos-10` = his `#f0faf1`; plus `--red-10` (dE 2.7), `--pos-100`, `--red-100`. For amber, rather than carry his orange-ambers as raw hex we **adopt JO'S OWN saffron vocabulary**, the four hexes already live in her file for her behind/pending states, promoted to `--sf-20:#fdeeba` / `--sf-60:#e3cf94` / `--sf-100:#f5b301` / `--sf-140:#c77d00` (that last being the same value as her own `--ptof-st-pend`). Role-matching, not hex-matching: the widget reads as hers, not as the prototype. The only three genuinely new steps, `--red-50:#e29f9f`, `--pos-30:#c9edce`, `--cn-90:#979797`, are values MISSING from ramps she already has, taken from `pathway-ds-main/tokens/pathway-design-tokens.json` (red-50, green-30, cool-neutral-90) so they are design-system values and not invented. **A finding worth recording: pathway-ds-main DOES ship full `saffron` (26 steps) and `orange` (29 steps) families; this shell simply never declared them, which is why W04/W17 concluded there was "no amber or saffron anywhere" and hard-coded.** All seven tokens are declared on `.fkp-band`, never `:root`, and the driver proves none is declared anywhere of hers. Our CSS declares ZERO of her selectors as a rule subject. Reuses her `.btn sm primary` for the red call to action, her delegated `data-tip`/`data-tip-plain` tooltip for the runway note (so **no native `title` attribute appears anywhere in the block**), her `ICON()` and her `money()` (whole dollars, which matches every figure in the spec). WHAT IT IS: four tiles, three static YTD figures with prior-year variance, and a dynamic Cash Position tile carrying the reconciliation warning; 2-colour severity by account count per spec 5a (amber under 3, red over 5); amber enumerates the unreconciled accounts as clickable rows with a right chevron and reddens a significantly overdue date, red shows one call to action instead because six rows is too many to list; **red does NOT suppress the runway figure, the deliberate v5 departure from v3, and the driver asserts both that the figure renders and that no suppressed dash appears**; his section 7 hover-persistence fix is carried exactly, a 200ms hide timer that a re-entry cancels, so the pointer can travel from the icon into the panel and click a row, with `Escape` and an outside click as the other two dismiss paths. All values are in the DOM as `.fkp-sr` text plus the visible figures, never hover-only. THE 3-TO-5 GAP IS PLACEHOLDERED, NOT FILLED: spec 5a leaves 3 to 5 accounts undefined and says the prototype treats them as amber, so `fkpSevOf` does exactly that AND `fkpUndefinedBand` surfaces a line on screen saying the treatment is not yet confirmed, rather than letting an unconfirmed rule look decided. Copy was rewritten to remove **7 em dashes and 1 en dash** from the donor's user-facing strings ("Total Income - YTD" became "Total Income (YTD)", "Jul 26, 2026 - 31 days ago" became "(31 days ago)", "May - Jul 2026" became "May to Jul 2026"). User-facing name is "Financial KPI"; the identifier W18 exists only in comments and internal names, and the driver asserts `W18` and `v5` never appear in rendered output. TWO REAL BUGS CAUGHT AND FIXED DURING VERIFICATION, both of which the isolated-block driver alone would have missed: (1) **the block was first appended AFTER the shell's closing IIFE**, which passed `node --check` and passed its own driver (the harness injects `ICON`/`money`/`setStatus`/`render` into the sandbox) but would have left all four OUT OF SCOPE in the real page; five of Jo's existing drivers caught it through their "the shell still ends in the render()/IIFE tail" assertion, and the block was moved inside the IIFE. This is the strongest argument yet for running the whole suite and not just the new driver. (2) two separate `click` listeners raced on the same event, the first opening the warning panel and the second closing it in the same tick, so the icon appeared dead; merged into ONE listener with the outside-close as its else branch. CARRIED, NOT RESOLVED: no Step 4 or Step 5 doc exists, so there is no design-decision or API grounding for any figure and **no `widget-diff-doc` section was written** (there is no Jo counterpart to diff against, `kind:"metric"` being only her generic "New widget N" placeholder); account-card and call-to-action navigation have no verified target or url pattern, so both report through `setStatus` and explicitly say the target is not yet defined (spec 11); the 30-day threshold, the 3-month averaging window and the whole account list are prototype fixtures with no API behind them (spec 11); the 3-to-5 band needs product confirmation; and the demo state chip is prototype-only per spec 6 and says so in its own tooltip. driver `w18-fkp.driver.js` **465/465** (incl. a 64-combination account-count x tooltip-state x demo-index no-em-dash-and-en-dash sweep, both hover-persistence directions with the timer asserted rather than waited on, all three dismiss paths, the severity ladder across 0/1/2/3/4/5/6/50, `cssDeclares` over all 28 classes the markup draws with, and byte-checks on Jo's render/contentHTML plus every kind-branch count). FULL SUITE AFTER THE EDIT: **4,668 assertions passed across 16 drivers, 0 failures attributable to this port.** Two drivers report one failure each, `cmp-dashboard` and `w17-gifts-mb`, both "the pre-edit snapshot is on disk to compare against"; **both were verified PRE-EXISTING by re-running them against the untouched `index.BACKUP-W18-20260915-112140.html` snapshot, where they fail identically.** Nothing published: no `git commit`, `git push` or `deploy.sh` was run. |
| W18 Financial KPI | rev 2 | fkp | verified | 9365ba2 | 2026-09-15 | **REV 2, three owner-reported corrections, same day.** (1) **THE WARNING PANEL WAS CLIPPED INSIDE THE CARD.** It was built as an absolutely positioned child of the cash tile at `z-index:40`, and was cut off by this block's own `.fkp-root{overflow:hidden}` , which had been copied faithfully from her `.widget` rule , and would in any case have been clipped by her `.appmain{overflow:auto}`. Fixed by moving it to its own `#fkpPop` appended to `document.body` at `position:fixed; z-index:3000`, which is **exactly what `.bkf-pop`, `.faf-pop`, `.apf-pop` and `.gpf-pop` already do in this file**, all four at the same z-index, so this is the house answer and not a new invention. The driver asserts the precedent explicitly by checking all four siblings are body mounted at 3000. Consequences handled: `.fkp-root` KEEPS `overflow:hidden` and stays faithful to her real `.widget` rule; the seven colour tokens moved to a shared `.fkp-band,.fkp-pop{...}` selector because the panel is no longer a descendant of the band and would otherwise have lost them; the panel is created and removed rather than shown and hidden, so `fkpClosePop` removes the element; positioning is right-aligned under the trigger, clamped on both axes and flipped above when there is no room below, the same geometry `bkFOpenPop` uses; a `#fkpWarnBtn` anchor id was added to the trigger to measure from; the panel carries `data-fkp="stop"` so our own outside-click ignores it; and `resize` and capture-phase `scroll` listeners reposition it so a fixed panel cannot float away from its icon. **The hover-persistence fix had to be generalised, and this is the subtle part:** the v5 behaviour depends on the pointer crossing from the icon into the panel without it closing, and the original check was `cash.contains(e.relatedTarget)`, which worked only while the panel was a DESCENDANT of the tile. Body mounting broke that containment, so the manager now recognises both surfaces by name via `fkpInSurface()` (`.fkp-cash` or `.fkp-pop`), and the driver asserts BOTH crossing directions (tile to panel, and panel back to tile) schedule no hide, plus that leaving both does. (2) **REFRESH ADDED**, reusing HER `.iconbtn` with her `refresh` glyph, her "Refresh, updated X" wording and her middle dot rather than a dash, and mirroring her own `refresh(id)` semantics exactly: loading state, one timer at 1100ms keyed in **her shared `timers` object** as `timers["fkp"]`, then ready with `updated` reset to "just now". It drops an open panel first, as her handler does `pop=null` before calling refresh. Loading renders per-tile shimmer bars rather than her `.skeleton`, because hers is a flex column built for a whole card body; the bars use her `--wn-200`/`--wn-300` tokens and respect `prefers-reduced-motion`. The driver asserts a refresh is the ONLY thing that enters the loading state: the demo chip and opening the panel are client-side and must not, which is the shell's loading convention. (3) **The "Year to date" subtitle was removed** by ruling, and its `.fkp-sub` CSS rule was deleted with it rather than left orphaned , the exact defect class this run reported in her own file (her `.ap-glance-cap` and `.gft-empty`), so the driver now asserts `.fkp-sub` appears in neither the CSS nor the markup. Diff vs the pre-port snapshot is now **716 added, 0 deleted**; `render`, `contentHTML`, `aboutOf`, `refresh`, `widgetHTML`, `ICON` and `money` are each still defined exactly once and byte-identical, and her `data-action="refresh"` count is unmoved since ours is `data-fkp`. FOUR DRIVER ASSERTIONS WERE SELF-CORRECTED RATHER THAN LOOSENED, all four mine being imprecise: an unanchored `/\.fkp-pop\{/` matched inside the shared token selector `.fkp-band,.fkp-pop{` instead of the rule being interrogated (re-anchored to a line start), and three "panel content is not in the band" claims tripped on the phrase "Unreconciled Account" appearing legitimately ONCE in the band, on the trigger's own `aria-label`; those now pin the occurrence count and assert the panel's structural classes (`fkp-tip-hd`, `fkp-acct`) are absent instead of forbidding the phrase. driver `w18-fkp.driver.js` grown to **851/851** (the sweep is now 128 combinations across count x panel state x loading x demo index, over band AND panel markup together). FULL SUITE: **5,054 assertions across 16 drivers**, the only two failures being the pre-existing missing-snapshot checks in `cmp-dashboard` and `w17-gifts-mb`, both verified identical against the untouched snapshot. Second snapshot taken as `index.BACKUP-W18rev2-*.html`. Nothing published. |

| W07 Deposits on Hand | deposits-oc | depO | verified | 9365ba2 | 2026-09-17 | **Account pop-up fix, plus driver renames.** The row-click account pop-up was failing in BOTH views, and both causes were ours. **THE ACCOUNT MODAL IS NOT JO'S AT ALL** , her dep widget has no account modal (her `dep*` functions stop at `depContent`, there is no `depAcctModalBody` or `depOpenAcct`), so the whole `.acctm*` family was invented with the depF/depO block, and three of its classes , `.acctm`, `.acctm-chart`, `.acctm-kpi` , **were never given a CSS rule at all**, the exact defect class this run has reported twice in her file (`.ap-glance-cap`, `.gft-empty`). (1) THE CHART COLLAPSED: the markup is `<div class="acctm-chart tr-plot">`, and with `.acctm-chart` undeclared the element inherited only `.tr-plot` (`flex:1; min-height:0; grid-template-rows:minmax(0,1fr) 18px`), which is built for a widget card where a flex parent supplies the height. In a modal there is no such parent, `flex:1` is inert, and the `minmax(0,1fr)` chart row resolved to ZERO; the 18px x-axis row kept its fixed height, which is why the week labels rendered while the plot did not, and why all three y-axis spans at `top:0/50/100%` stacked onto one line. The markup was correct throughout: rendering the body in Node produced a valid `<polyline>` with real coordinates and three y-labels, so this was never a logic bug. (2) THE TABLE OVERFLOWED AND WAS CLIPPED: her generic `.modal` is `max-width:390px`, less `.modal-b`'s 18px side padding = 354px of content, while the shared `.wt-row` columns are sized for a widget card and **none can shrink** (`.dt-type` 180 + `.dt-tr` 84 + `.wt-c2` 96 + 3 gaps of 12 + 28 row padding = **424px of fixed width**, 70px past the container before the Name column gets a pixel), and `.acctm-tbl{overflow:hidden}` cut the remainder. FIRST ATTEMPT WAS REJECTED AND IS WORTH RECORDING: squeezing her shared column widths under `.acctm-tbl` scoped overrides. Owner directed a duplicate shell instead, which is better and is already this file's own pattern , `remOPledgeModalHTML` builds its own `modal modal-wide rem-drill-modal` for exactly this reason. So `depOAcctModalHTML()` now duplicates her generic modal markup (same backdrop, same `data-action="modal-close"` / `stop`, same ARIA, same "Got it" footer) under our own `.depo-acct-modal` class at **560px**: 524px of content holds her 424px of columns with 100px left for the name, so **her `.wt-row` / `.dt-*` / `.wt-c2` widths are used exactly as she wrote them and nothing of hers is re-scoped**. Cost in her code is **one appended dispatch line** in `renderModal` keyed to our own `modal.type==="depOacct"` (byte-diffed: her function went 28 lines to 29, one added, nothing else changed), and her 390px default is untouched for every other generic modal. ISOLATION WAS THE OWNER'S SECOND CORRECTION and it mattered: `.acctm*` is shared with the older `dep` and `depF` account bodies (three `class="acctm"` sites exist in the file), so the first draft's unscoped `.acctm{...}` / `.acctm-kpi{...}` / `.acctm-chart{...}` would have silently restyled both of those too. Every rule is now scoped under `.depo-acct-modal`, which only our own shell emits, and the driver asserts zero unscoped `.acctm` rules exist, that at least four of ours are scope-prefixed, and that exactly one site emits the scope class. Also renamed **three misnamed drivers**: `w05-receivables-mb`, `w06-insurance-mb` and `w07-deposits-mb` were all actually the `-oc` drivers (their own headers said so, they call `extractRegistry(S,"<kind>-oc")`, and they carry 417 `arO` / 91 `insO` / 148 `depO` references and **zero** to the `-mb` prefixes); renamed to `-oc` and the 7 stale references updated across the build sheet, both difference docs and this manifest. The genuine old `-mb` drivers survive as `.v2mb.bak`. driver `w07-deposits-oc.driver.js` grown 222 -> **263/263**. FULL SUITE **5,110 assertions across 16 drivers**, only the two pre-existing missing-snapshot failures (`cmp-dashboard`, `w17-gifts-mb`), both verified identical against an untouched snapshot. NOT ADDRESSED, deliberately: the superseded `-mb` blocks remain live and dispatched (`receivables-mb` 7 cards, `insurance-mb` 6, `deposits-mb` 6, and the rest) with no driver covering any of them, since each `-mb` driver became its `-oc` replacement; the same two undeclared-class bugs are therefore still present in the `depF` pop-up, left alone per the owner's "only for my widget". Nothing published. |

| W08 My Status | mystatus-oc | mysO | verified | 9365ba2 | 2026-09-17 | **THIS ROW REVERSES A RECORDED RULING, deliberately and on owner instruction.** Both this manifest's header and the port skill state that W08 My Status is Jo's own, completed by her, never ported and never modified (ruling 2026-09-07). The owner directed a duplicate on 2026-09-17 with a specific reason: **her W08 is finished and is the best of the pair, and the clone exists purely as a sandbox to trial small improvements against it without risking hers.** What the reversal does NOT change: her widget is COPIED, not modified, and the driver proves it. W14 Main Content Tasks remains hers alone and is still not ported. WHAT IT IS: a behaviour-for-behaviour clone, not a redesign. All ten of her functions, her three data constants, her 58 CSS rules, her eleven click branches, her two modal types, her options popover and her config search input were cloned and mechanically renamed; **there are no design changes at all, by intent**, so that when the owner's improvements land, every diff against hers is a real decision rather than clone drift. PARITY IS ASSERTED AS STRICTLY AS ISOLATION: rendering our card and renaming our namespace back to hers produces markup **byte-identical to hers at both Explore and Detail**. NAMESPACE, each of which had **ZERO** occurrences before the clone: `mysO*` / `MYSO_*` / `.myso-*` / `myso-*` action values / `mystatus-oc` / `mysOconfig` + `mysOdetail` modal types / `myso-opts` popover type / `mysOConfigQ` input id. Her action prefix cannot collide with ours because hers ends in a hyphen, so it never prefix-matches ours, and the driver asserts that property directly rather than assuming it. HER CODE, BYTE-VERIFIED AFTER THE EDIT: her data block (6,492B), her render block (8,362B) and her `.mys-*` CSS cluster (4,069B) are all **md5-identical** to the pre-clone snapshot; all ten functions defined exactly once with byte-identical opening lines; her three constants declared once; her two registry rows byte-identical; all eleven `data-action="mys-*"` emitted counts unmoved; her `contentHTML`, `popContent` and both `renderModal` dispatches intact. Our regions declare zero of her selectors and contain zero of her identifiers. SHARED PLUMBING, ON PURPOSE: a clone that diverges in its wiring is no longer a clone, so this uses her `pop` / `modal` / `render` plumbing rather than a private subsystem, reached the way her own widgets reach it , through ONE delegating line each, exactly as `insHandleClick` / `apHandleClick` / `gftHandleInput` already are. Seven appended lines total: `contentHTML`, the click listener, the input listener, `popContent`, `triggerSelector`, and two in `renderModal`. Nothing of hers is rewritten. **OUR CONTROLS CARRY `data-action`, NOT A PRIVATE ATTRIBUTE, AND THAT IS LOAD BEARING:** her click listener nulls any open `pop` when a click misses `[data-action]`, so a private attribute would have let her listener close our popover before ours could toggle it, and it could never have shut , the same race class as the W18 double-listener bug. A first draft did use `data-myso` and the parity check caught it as a byte diff, which is exactly what parity checking is for. FIVE REAL PROBLEMS FOUND AND FIXED DURING THE RUN, all ours: (1) `cmpCards` collapsed any object-valued state to `{}` so three sizes of a widget could not share a drill map , but an **array is also `typeof "object"`**, and W08 is the first row whose state is a list (`selected`), so its query list arrived as `{}` and `mysContent` threw on `.map`; arrays are now copied per card, which is the same isolation, and no earlier row had an array so nothing else moves. (2) `MYSO_DEFAULT` was `undefined` when `CMP_ROWS` evaluated it, because a `var` declared in a late region is hoisted but not yet assigned; our data block was therefore lifted up beside hers, which is why her own data block sits early too. (3) Two of our header comments quoted her `MYS_`, `.mys-` and `"mys-` tokens verbatim, tripping our own isolation guards , the exact W13/W15/W16/W17 hazard, and the comments were **reworded rather than the guards loosened**. (4) Our note-card entry used a `lead:` key where `cmpNoteContent` reads `n.h`, so the summary silently rendered nothing. (5) Her `renderModal`/`renderOverlay` need real DOM nodes and the bare `innerWidth` global, so the driver seeds them rather than skipping the paths that call `render()`. THE SIDE BY SIDE TAB: W08 is appended as a **five-card group, not seven** , hers at Explore and Detail, ours at the same two, plus the What-changed note. No Glance card is invented, because her registry declares `tiers:["wide","xwide"]` and her `mysContent` does not branch on size at all, so a Glance card would show a view she never designed; `cmpCards` gained an optional per-row tier list for this, and every other row omits it and still gets three. The tab is now **103 cards**. Four `cmp-dashboard` assertions encoded the old ruling (98 cards, and `mystatus` asserted ABSENT as "hers alone") and were updated to the new one, with the `tasks`/W14 half kept exactly as it was. driver `w08-mystatus-oc.driver.js` **244/244**; `cmp-dashboard` grown to **2,001** passing. FULL SUITE **5,354 assertions across 17 drivers**, the only two failures being the pre-existing missing-snapshot checks in `cmp-dashboard` and `w17-gifts-mb`. Diff vs the pre-clone snapshot: **286 added, 5 deleted**, and all five deletions are lines we replaced in our own code or appended a dispatch to, none of them hers. **CLOSED 2026-09-17 with TWO improvements, both CSS or copy only, both scoped to our clone, and her build md5-identical after each.** IMPROVEMENT 1, the pop-up content fills the pop-up: the shells were never the problem and were left alone (both already carry her `modal-wide`, and `.modal.modal-wide{max-width:1080px}` at (0,2,0) already outranked the cloned `.myso-*-modal{max-width:720px/760px}` at (0,1,0), so those caps were never winning). The fault was that `modal-wide` is a flex column at `height:84vh` while the inner scrollers stayed pinned at `max-height:320px` (config) and `60vh` (records), so the content stopped short and the rest of the dialog was dead space; `.modal-b` is now the flex child that owns the leftover height and both scrollers are released to fill. A first pass started lifting the dialog WIDTH and was corrected by the owner: "the inner text is the issue, not the pop up themselves", and the width work was removed. IMPROVEMENT 2, the urgency dots are explained: there are only **two** values, `action` (11 queries, `--red-100`) and `info` (10, `--cn-110`), and **nothing named either of them anywhere** , no legend, no tooltip, and every dot carried `aria-hidden`, so the meaning reached neither the eye nor a screen reader. A `MYSO_URGENCY` label map now feeds a quiet two-key legend placed directly ABOVE the available list (next to the dots it explains, rather than a search box away), reusing the rows' own `.myso-dot` swatches so the key cannot drift from what it labels, and all 21 rows name their urgency in their accessible name via her existing `.sr-only`. Isolation asserted both ways: her Configure modal gains no legend, no `.sr-only` labels and no `MYS_URGENCY`, and her 21 dots are still `aria-hidden` exactly as she wrote them; her four sizing rules (720px / 760px / 320px / 60vh) are untouched and asserted. Because both improvements are CSS and copy, the **markup-parity assertions against her widget still hold**. A THIRD finding was reported and deliberately NOT acted on, per "I just want ideas, not changes": **the module grouping in her widget is half-built and dormant** , every query carries `area:` (GL 9, HR 8, AP 2, PUR 2), the registry sets `mode:"urgency"`, a `mys-mode` click branch exists and `.mys-modetog`/`.mys-modewrap` are declared, but **`w.mode` is read zero times and no control anywhere emits `mys-mode`**, so the state, handler and styling for an Urgency/Module toggle all exist while the toggle itself was never rendered. Eight options for surfacing module were given; none built. driver grown to **291/291**; suite **5,401**. Nothing published. |

## Run complete , 2026-09-08

**All fourteen ported widgets are verified and diff-doc'd on branch `oisin-v2-rebuild` (base `71ca056`), and W17 Gifts Pledges closes the run.**

FINAL FOURTEEN-DRIVER TALLY, every driver green: W01 140, W02 159, W03 277, W04 329, W05 288, W06 280, W07 224, W09 413, W10 228, W11 435, W13 763, W15 463, W16 740, W17 734 = **5,473 assertions, 0 failures**.

Fourteen widgets ported (W01-W07, W09, W10, W11, W13, W15, W16, W17). W08 My Status and W14 Main Content Tasks are Jo's own, completed by her, and were never ported or modified. W12 has no port on this branch.

Jo's code was never modified beyond the two dispatch lines per widget (`contentHTML` and `aboutOf`). Every widget's block is additive and namespaced, her helpers are byte-verified unchanged after each port, and each port's CSS declares zero of her selectors as a rule subject.

**Nothing is published.** The working tree on `oisin-v2-rebuild` is changed on disk only; the owner makes one commit at the end. No `git commit`, `git push` or `deploy.sh` was run at any point in this run.


## Comparison dashboard , added 2026-09-08

**A fourth tab, "Side by side (Jo vs OC)" (`id:"cmp"`), for reviewing every port against Jo's original in one place.** It is a review surface, not a widget: it consumes the diff docs, it is not one of them, so no widget section was added to `Design Differences (Jo vs Oisin)/`.

**98 cards = 14 widgets x 7.** Per widget, in order: HERS at Glance / Explore / Detail, OURS at Glance / Explore / Detail, then one diff-summary note card. Widgets covered: W01-W07, W09, W10, W11, W13, W15, W16, W17. W08 My Status and W14 Main Content Tasks are Jo's own and W12 is an empty slot, so there is nothing to compare for any of the three and none is listed. Every card carries `tiers:["kpi","wide","xwide"]` and `goto:false`.

**Jo's three dashboards are byte-identical.** The tab is appended as a fourth array element, so `d1` stays the default. The edit is purely additive: 92 lines inserted, 0 deleted, 0 rewritten, CRLF throughout.

**The cards are BUILT, not typed as registry literals.** `CMP_ROWS` holds one spec row per widget (number, name, her kind + her state, our kind + our state) and `cmpCards()` expands it. That is deliberate: typing 84 registry rows would have moved the `kind:"..."`, `id:"..."` and shared-constant occurrence counts that the fourteen widget drivers pin against their own pre-edit snapshots, and it would have blown the W17 driver's added-line budget. Object-valued state keys (the empty expand and page maps) are cloned per card so three sizes of one widget cannot share a drill state.

**Where her state keys came from.** Seven of her fourteen widgets still have live registry rows (pto, loans, fixedassets, purchasing, bank, payables, gifts): their keys are copied verbatim from her own live entry, asserted key by key against the live runtime object. The other seven (budget, pension, payroll, remittance, ar, insurance, deposits) she deleted from her own dashboard in her commit `faa6507`; their keys are copied from **her own entries at `faa6507~1`** and then verified by rendering, not by assumption: for each, the driver proves the key is genuinely read (flipping it changes the output) and that the value we carried is her own default (the card renders identically to a card with no state at all). **This tab is now the only thing in the file that exercises those seven render functions.** All fourteen of hers render at all three sizes; none had to be left out.

One deliberate omission: her gifts receipts-through key is not set on the comparison card. Her own accessor already falls back to the file's gifts date anchor, the rendered card is byte-identical either way (asserted at all three sizes), and naming that key here would have moved a raw occurrence count her W17 driver pins to its snapshot.

**The new kind: `cmpnote-mb`.** One small additive block, 92 lines total across four insertion points: a `CMPNOTE_` lookup keyed by widget number (14 summaries, each a framing line, 6 to 7 difference rows and that widget's open item, condensed from `Design Differences (Jo vs Oisin)/Jo vs Oisin - Design Differences.md`), the `CMP_ROWS` spec, `cmpCards()`, one render function `cmpNoteContent(w)`, one dispatch line appended at the END of the `contentHTML()` if-chain, and **7 CSS rules** (all `.cmpnote-` scoped, brace-balanced, redeclaring nothing of hers). Static text only: no interactions, no popovers, no handlers, no timers, no `data-action`, no buttons, no SVG. It reuses her `.cap` caption and her `.state` empty block; the rest is our own `.cmpnote-*`. `cmpnote`, `CMPNOTE_`, `cmpCards`, `cmpNoteContent`, `CMP_ROWS`, `id:"cmp"` and `Side by side` each had **zero** occurrences before the edit.

**Driver: `cmp-dashboard.driver.js`, 1,872 / 1,872 assertions, all passed.** It runs the WHOLE shell script on the shared harness (`loadShell` + `runBlock` + `Assert`) rather than one extracted block, because `contentHTML()` and all fourteen of her entry functions have to be live at once. It asserts the tab's shape and card count, per-group ordering and titles, that **every one of the 98 cards renders non-empty at its declared size** for hers and ours alike (with no card silently degrading into a skeleton or an error state), that each note card puts its framing line, every row and its open item on screen, that her three dashboards are byte-identical to the snapshot, that every widget's live registry row count and `extractRegistry` result are unmoved, that the dispatch is the last kind branch in the chain, and a no-em-dash sweep over all 98 cards, title and body.

**All fourteen widget drivers re-run green afterwards, unchanged: 5,473 assertions, 0 failed. Suite total with the new driver: 7,345 assertions, 0 failed.** Syntax gate `node --check` on the extracted script passed after every edit. Nothing is published: no `git commit`, `git push` or `deploy.sh` was run.
