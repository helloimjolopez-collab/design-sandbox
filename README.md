# Dashboards Sandbox

Widget and dashboard prototypes, published to GitHub Pages and auto-deployed on save.

**Live:** https://helloimjolopez-collab.github.io/design-sandbox/

## What's here

| | |
|---|---|
| `Widget Container Demo/` | Dashboard + Widget depth ladder (Pathway draft v14) — `main` |
| `phase-2` branch | The same demo on the phase-2 branch, served at `/phase-2/` |

`.github/workflows/pages.yml` (on the `phase-2` branch) assembles `main` at the site
root and `phase-2` at `/phase-2/`.

**These links are in active use by external collaborators. Do not rename this
repository** — renaming changes the Pages URL and breaks every link already shared.

## Everything else

The other demos, prototypes and audits that used to live here are now private Claude
Artifacts, indexed from a single front page. The full original history of this repo,
including everything removed, is archived privately at
`helloimjolopez-collab/jo-design-sandbox`.
